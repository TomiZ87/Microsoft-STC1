/** @type that contains prototype of one boolean and number variable */
type nums = {
    MAX_SAFE_INTEGER: number;
    caseinArray: boolean;
}
/** Exports the @type, so it can be used in another file in the project */
export default nums;